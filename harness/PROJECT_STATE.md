# Project State — FinPulse Learning MVP

Last updated: 2026-06-08

## Current phase

Stage 2 backend MVP is on `main`. The current workspace contains stacked review changes through T-105, including learning path, entry auth, content API delivery, PostgreSQL persistence, private reflection/artifact answer persistence, production deploy work, the 2026-06 methodology reset, the first four active T1 runtime lessons split across the first two T1 units, lesson-reader layout polish, learner-facing wording cleanup, custom reflection option input, artifact variant copy/alignment polish, artifact micro-rule custom radio options, the selected explanatory calculation block pattern for passive lesson cards, the approved vertical mobile layout for calculation steps, container-aware formula layout for narrow lesson cards, objective `multi_select` / `categorization` practice card support, scoped card-flow practice UX for objective third-screen exercises, production rollout of the approved auto-advancing categorization flow to current third-screen categorization exercises, the first-screen lesson goal card replacing the duplicated description/goal brief, the emergency-fund practice card converted to the same two-category auto-flow categorization pattern, the reserve calculation practice converted to a three-example `Верно` / `Есть ошибка` categorization flow, no-break rendering for financial amount/unit fragments in lesson-card text, the Unit 2 screen-four personal-data pattern alignment, the Unit 2 final micro-rule screens aligned to variants plus `Свой вариант`, the shared textarea-like custom-option field for reflection/artifact `Свой вариант` choices, the first four T1 lessons expanded to eight screens with an external-example `scenario` plus statistics on screen 4, T-101 cleanup of obsolete experiment routes/pages, stale loaders/helpers, unused dependency/scaffold files, and local artifacts, T-102's standalone lesson-completion experiment page, T-103's fifth user-directed hybrid completion variant, T-104's production application of that completion variant with the experiment route removed, and T-105's app-facing brand-name replacement from `FinPulse` to `ФинПульс`. Historical review notes may still mention removed design preview pages.

T-063 made the new methodologist-provided methodology the active central source at `docs/methodology/METHODOLOGY.md`, removed the old active Finzdorov/AI/personal-experience methodology directories from the active system, and replaced runtime content with the first target lesson graph. T-064 extended that active graph with three additional lessons, and T-065 split the two emergency-fund lessons into their correct second runtime unit.

T-108 aligned the local harness with the accepted T1 eight-screen lesson architecture: `docs/methodology/AUTHORING.md` and `docs/CONTENT_MODEL.md` now state the exact screen-by-screen contract, builder/orchestrator/subagent/verifier prompts point content agents to that contract, and `npm run check:content` enforces it for active T1 runtime lessons.

The app scaffold exists as a Vite React TypeScript SPA with Tailwind CSS, shadcn/ui, React Router, Vitest, and a mobile content-reader surface. Runtime content now uses split JSON files with the hierarchy Program -> Module -> Unit -> Lesson -> Card. ADR-0006 accepts a narrow Stage 2 backend: Fastify, simple learner login, httpOnly cookie sessions, backend-owned progress markers, and read-only content API delivery from validated JSON. ADR-0007 accepts private persisted reflection/artifact answers for authenticated learners only. ADR-0008 supersedes the SQLite persistence portion of ADR-0006 with PostgreSQL behind async repositories.

The Fastify backend and frontend API migration are implemented on `feat/stage-2-backend-mvp`. Frontend rendered pages now fetch program/module/unit/lesson data through `/api/**`; authenticated users can save viewed/completed lesson/card progress and private reflection/artifact answers. Security/content contract reviews, `npm run verify`, and browser smoke passed.

## Locked MVP assumptions

- Mobile-first educational site.
- Static educational program content.
- JSON as source-of-truth.
- React + TypeScript.
- SPA/Vite preferred.
- Fastify backend accepted for Stage 2.
- PostgreSQL persistence accepted for Stage 2 learner/session/progress/reflection state.
- React state first; add Zustand only if small cross-route client state becomes necessary.
- Tailwind + shadcn/ui.
- Minimal learner login is allowed for saved progress; full user cabinets remain out of scope.
- Authenticated learner reflection/artifact answers are allowed as a private personal artifact, not diagnostics, scoring, analytics, or recommendations.
- The selected mascot visual reference is a cream/sky-blue fennec or fox guide with a compass badge; mascot-led mechanics remain out of scope.
- No diagnostics.
- No rewards.
- No analytics dashboards.
- Backend-owned progress is not diagnostics, scoring, analytics, or recommendations.
- JSON remains the canonical educational content source-of-truth.
- Evals deferred until product flows exist.
- Agent model policy: GPT-5.5, reasoning effort `xhigh`.
- Branch, commit, push, and PR rules live in `docs/engineering/contributing.md`.

## Known open questions

- Final product name and broader visual identity beyond the selected mascot reference.
- Exact content taxonomy beyond the initial lesson block types.
- Deployment target.
- Production secret/session hardening and rate limiting.
- Export/delete controls and richer metadata for saved personal answers.

## Current verification state

`./scripts/verify.sh` exists as the generic entry point and runs content validation, runtime content import guard, typecheck, lint, tests, production build, and Storybook build through available package scripts.

`npm run dev` now starts both the Fastify backend and Vite frontend. Vite proxies `/api` to `http://127.0.0.1:3001` for local development. The backend uses PostgreSQL for learner-owned state and defaults to `postgres://finpulse:finpulse@127.0.0.1:5432/finpulse` outside production unless `FINPULSE_DATABASE_URL` or `DATABASE_URL` is set.

GitHub Actions runs `npm ci` and `npm run verify` for pull requests and pushes to `main`, with a PostgreSQL service available to backend tests through `FINPULSE_TEST_DATABASE_URL`.

`src/content/program.json` is a program manifest. Module and unit runtime content live under `src/content/modules/**`. The content validator also validates the example split graph, rejects unknown keys, requires normalized relative JSON paths, checks sorted unique `order` values, and verifies scenario `correctOptionId` values have matching options.

Active runtime content now has one tier/module, two units, and four lessons:

- module slug `t1-start`, title `T1 Старт`;
- unit slug `money-and-operations`, title `Юнит 1. Деньги и операции`;
- `money-and-operations` lessons:
  - lesson slug `where-money-goes`, title `Куда уходят деньги`;
  - lesson slug `mandatory-and-desired`, title `Обязательное и желаемое`;
- unit slug `planning-and-management`, title `Юнит 2. Планирование и управление`;
- `planning-and-management` lessons:
  - lesson slug `why-emergency-fund`, title `Зачем нужна подушка`;
  - lesson slug `reserve-amount`, title `Сколько держать в резерве`.

Legacy runtime slugs from the previous content graph are intentionally removed from active content and should return 404 through the content API. Historical task notes may still mention old slugs as an audit trail.

T-009 interactive lesson cards merged in PR #4. Current interaction state remains local React component state only; no backend, accounts, diagnostics, rewards, analytics, or persistence were added.

T-010 accepted ADR-0006 for Stage 2 backend scope before backend implementation. T-011 added the Fastify + SQLite auth/content/progress API. T-012 migrated rendered frontend data to the backend API and wired minimal auth/progress controls. Full freeform lesson answers remain transient; only viewed/completed lesson/card progress is in Stage 2 persistence scope.

T-013 verified Stage 2 with `npm run verify`, security/auth review, content/API contract review, live browser smoke, DB progress persistence check, and 360px no-overflow check.

T-014 added shared content API contract tests and a runtime content import guard. `npm run verify` now also runs `npm run check:runtime-imports` to keep rendered app code on backend API data instead of direct JSON/runtime loader imports.

T-015 adapted the friendly-learning design-system draft to FinPulse. `docs/DESIGN_SYSTEM.md` now defines MVP-safe lesson/card experience guidance and keeps rewards, streaks, challenges, shops, mascot-led experience, and retention loops deferred.

T-016 implements the first friendly-learning lesson/card UI slice: one active card per lesson session, a progress header, sticky bottom CTA, supportive choice feedback, local checklist/reflection/artifact interaction state, and existing viewed/completed progress markers only. `npm run verify` passed; browser smoke passed on desktop-ish and 360px with no horizontal overflow or console errors.

T-017 reworks the rendered frontend into a guided learning path without changing JSON/content/API contracts: the program overview emphasizes the current module, next step, progress, and lesson path preview; `/modules/:moduleSlug` renders units inline as a vertical lesson path; `/modules/:moduleSlug/units/:unitSlug` remains a compatible focused unit path; lesson checked-answer feedback is shown in the sticky bottom action area above `Далее`. Future/locked states are visual guidance only, not access control. `npm run verify` passed; 390px browser smoke passed for home, module path, and checked-answer lesson feedback with no horizontal overflow or console errors.

T-018 adds Storybook as a separate hosted UI catalog for foundations and current FinPulse components. The learner SPA remains at `/`; Storybook is built as a static artifact into `dist/storybook/` for hosting at `/storybook/` and is not a React Router route. `npm run verify`, `npm run build:storybook`, `npm run build:all`, and local Storybook browser smoke passed.

T-019 replaces the legacy accent token family with `sky`: `--fr-color-sky-400: #5BC0EB`, `--fr-color-sky-500: #1E9BD7`, and `--fr-color-sky-600: #1479B8`. Direct UI and Storybook token references now use `--fr-color-sky-*`; `npm run verify` passed.

T-020 changes the root entry route: `/` now renders login/registration for anonymous users after the session check, and a welcome continuation screen for authenticated sessions. The learning path/program overview remains available at `/program`; direct module, unit, and lesson URLs remain public and compatible. Auth form markup now uses shadcn field/input primitives. `npm run verify` passed; Browser smoke passed for anonymous entry, registration-to-welcome, `/program`, and 390px mobile auth layout with no console errors.

T-021 adds an inline video-card renderer without changing JSON/content/API contracts. Supported RUTUBE embed URLs now render inside the lesson reader with timecode jump buttons, a source fallback link remains visible, and unsupported video URLs fall back to the external source link. `npm run typecheck`, focused lesson-card tests, `npm run verify`, and Browser smoke passed.

T-022 fixes local registration `Failed to fetch` when the frontend uses a direct API base URL from a loopback origin such as `http://127.0.0.1:5174`. The backend now allows local loopback browser origins by default when `FINPULSE_CORS_ORIGIN` is empty or unset, while explicit env/config origins still override the default. `npm run test:run -- server/app.test.ts`, `npm run verify`, and Browser smoke for direct API registration passed.

T-023 fixes email-style auth identifiers: registration/login now accepts email addresses in the existing `login` field, normalizes them to lowercase, and keeps username-style logins supported. The auth form label now says `Email или логин`. `npm run test:run -- server/app.test.ts src/features/auth/AuthControls.test.tsx`, `npm run verify`, and Browser smoke on `http://127.0.0.1:5174/` passed.

T-024 fixes the remaining main-screen `Failed to fetch` banner after authenticated lesson navigation. The root cause was progress persistence using `PUT /api/progress/**` while backend CORS only advertised `GET,HEAD,POST`, so browsers blocked the actual progress `PUT` after preflight. CORS now allows `GET, POST, PUT, OPTIONS`. Lesson/card viewed-write callbacks are also stable and guarded to avoid duplicate dev StrictMode progress writes. `npm run test:run -- server/app.test.ts src/App.test.tsx`, `npm run verify`, and Browser smoke on `http://127.0.0.1:5174/` passed.

T-025 adapts the Lingvo-like navigation concepts into the frontend without changing JSON/content/API contracts: `/program` now emphasizes module cards; module and unit routes render color-sectioned lesson maps; lesson nodes open a detail popup with title, description, metadata, and an explicit navigation action; the module path ends with a next-module/finish transition card. `npm run test:run -- src/App.test.tsx src/features/program-navigation/learningPath.test.ts` and `npm run verify` passed. Browser DOM/interaction/mobile smoke passed for `/program` and `/modules/financial-goals`; in-app screenshot capture timed out, so rendered evidence is DOM/layout/console based.

T-026 cleans visible UI noise from the learner SPA without changing JSON/content/API/backend contracts: entry and program screens use quieter progress/copy, module/unit paths remove redundant content/back-to-top/unavailable-next controls, lesson nodes hide future-state labels while keeping accessible names, lesson dialogs and CTAs drop decorative icons, and the lesson reader removes duplicate card counters/type pills and local-only helper copy. Empty draft statuses remain available to assistive tech and become visible after user action. `npm run test:run -- src/App.test.tsx src/features/lesson-reader/LessonCardRenderer.test.tsx src/features/auth/AuthControls.test.tsx` and `npm run verify` passed. Browser smoke at 390px passed for `/`, `/program`, `/modules/financial-goals`, `/lessons/why-values-matter`, and checked-answer feedback with no console errors or horizontal overflow; Browser screenshot capture succeeded.

T-027 previously added a second runtime content unit to Module 1, `unit_02_impulsive_purchases`. T-034 superseded that runtime addition at the time: the unit and `pause-before-purchase` lesson were removed because they did not belong to the prepared Finzdorov Module 01 source scope.

T-028 documents the user-provided mascot visual direction without implementing it in the UI: `docs/MASCOT.md` defines the cream/sky-blue fennec or fox guide, compass badge, approximate palette, anatomy, usage rules, asset requirements, and product boundaries. `docs/DESIGN_SYSTEM.md` now treats the mascot as an optional visual identity asset while keeping mascot-led mechanics deferred. `npm run verify` passed.

T-030 simplifies the lesson detail popup opened from the module/unit path without changing content, API, auth, progress, or routing contracts. The popup now keeps the lesson title, duration with an icon, and actions only; the module/section label, lesson description, and main-skill block were removed to avoid clipped mobile text. `npm run test:run -- src/App.test.tsx`, `npm run verify`, and Browser mobile/desktop popup smoke passed.

T-031 turns `docs/methodology/README.md` into a maintainable methodology source catalog under `docs/methodology/finpulse_methodology/`, preserving the full original in `00_original_content.md`, and adds `docs/methodology/CONTENT_BACKLOG.md` with runtime/supplemental/methodology/future-scope/schema-gap classifications. T-034 supersedes the former Module 2 runtime slice from T-031: `budget-without-shame` is removed from runtime because it is outside the current factual Finzdorov Module 01 / block `01.01` scope.

T-032 adds responsive application navigation without changing content, API, auth, progress, or routing contracts. Desktop now uses a fixed left sidebar with FinPulse branding, in-scope learning/account navigation, active states, and logout access for authenticated users. Mobile non-lesson routes use a fixed bottom menu; lesson routes keep the bottom menu hidden to avoid overlapping the lesson CTA. The authenticated welcome entry also exposes logout now that the old top header auth controls are gone. `npm run test:run -- src/App.test.tsx`, `npm run typecheck`, `npm run lint`, `npm run build`, `npm run verify`, and Browser desktop/mobile smoke passed.

T-033 adds the approved FinPulse mascot asset and UI rollout without changing content, API, auth, progress, or routing contracts. The supplied source is kept as a transparent source copy at `public/assets/mascot/finpulse-mascot-source.png`; the transparent runtime PNG is `public/assets/mascot/finpulse-mascot.png`; `src/shared/ui/Mascot.tsx` centralizes the asset path, alt/decorative behavior, variants, and fixed sizes. Mascot placements now appear as a calm financial navigator in entry/loading/welcome states, program overview route guidance, module header/path dialog/transition/empty states, lesson intro/completion/empty cards, and only supportive lesson feedback tones. `npm run verify` passed; Browser desktop/mobile smoke passed with asset loading confirmed, no console errors, and no 390px horizontal overflow.

T-034 aligns runtime content with the factual methodology currently available for `01.01 Ваши базовые ценности`. The program now has one module, `financial-goals`, and Module 1 has one section/unit, `01.01 Ваши базовые ценности`, containing the existing eight adapted lessons from `why-values-matter` through `practice-1m`. Former agent-added runtime content outside that block (`impulsive-purchases`, `pause-before-purchase`, `budget-without-shame`, `budget-as-choice-map`) is removed; API tests assert those routes are no longer served.

T-035 adds `docs/methodology/AUTHORING.md` as the unified Russian-language authoring framework for turning provided source content into FinPulse-ready methodical packages and runtime JSON. The guide consolidates MVP boundaries, source intake, fragment classification, lesson method cards, supported card types, adaptation rules, QA checklists, file package structure, and verification expectations. No runtime JSON, schema, UI, backend, auth, or progress contracts changed. `npm run verify` passed.

T-036 adds the supplied FinPulse wordmark to desktop navigation without changing content, API, auth, progress, or routing contracts. The baked-checkerboard source PNG was converted into `public/assets/brand/finpulse-wordmark.png` as a transparent cropped runtime asset, and the desktop sidebar brand link now renders that wordmark at stable intrinsic dimensions. `npm run test:run -- src/App.test.tsx`, `npm run verify`, and Browser desktop smoke on `/program` passed.

T-037 polishes the account/logout navigation surface without changing auth, API, content, progress, or product scope. The desktop sidebar now treats the account area as a bottom dock with the signed-in learner and logout action; the mobile bottom menu now shows account/login/logout affordances according to auth state; the authenticated welcome screen no longer duplicates the logout button. `npm run test:run -- src/App.test.tsx`, `npm run verify`, and Browser desktop/mobile smoke passed.

T-038 adjusts account/logout placement after UI review without changing auth, API, content, progress, or product scope. Desktop now separates `Аккаунт` as its own sidebar navigation item from the bottom `Выйти` action. Mobile bottom navigation no longer contains `Выйти`; the authenticated `/` account/welcome section renders a mobile-only logout button at the bottom of its content. `npm run test:run -- src/App.test.tsx`, `npm run verify`, and Browser desktop/mobile smoke passed.

T-039 polishes the highlighted circular lesson nodes without changing content, API, auth, progress, routing, or product scope. Current/completed lesson nodes now use a clipped diagonal glare that spans the whole circular button, closer to the Lingvo-style reference. `npm run verify` and Browser desktop/mobile smoke on `/modules/financial-goals` passed.

T-040 removes the broad mascot placements from current learner UI surfaces without changing content, API, auth, progress, routing, or deleting the approved mascot asset/component/docs. `src/shared/ui/Mascot.tsx` and `public/assets/mascot/**` remain available for later user-approved placements. Current app routes no longer render mascot images in entry/account, program, module path/dialog/transition/empty states, lesson intro/completion/empty states, or feedback. `npm run verify` passed; Browser desktop and 390px smoke passed for `/`, `/program`, `/modules/financial-goals`, `/lessons/why-values-matter`, and the module lesson dialog with no console errors, no horizontal overflow, and zero rendered mascot images.

T-041 converts extracted Finzdorov sections `01.02 Видение будущего`, `01.03 Финансовые цели`, and `01.04 Мотивация достижения целей` into runtime JSON without changing schema, UI renderers, backend API, auth, or progress contracts. Module 1 now has four units: `values-and-goals`, `future-vision`, `financial-goals-map`, and `goal-motivation`, adding eleven short lessons from `life-cycle-and-money` through `goal-levels`. Source mapping docs live under `docs/modules/module_1/lesson_02_future_vision/`, `lesson_03_financial_goals/`, and `lesson_04_goal_motivation/`; large PDFs, XLSX, images, and templates are preserved as `supplemental`. `npm run check:content`, focused API/app tests, `npm run verify`, and Browser 390px smoke passed.

T-042 aligns the visual module path with the runtime unit structure without moving or mixing content. `buildLessonPathSections` now creates one visual section per unit and uses `unit.order` for section numbers, so Module 1 renders as `01.01`, `01.02`, `01.03`, and `01.04` instead of splitting `01.01` into additional artificial chunks. `npm run test:run -- src/App.test.tsx src/features/program-navigation/learningPath.test.ts`, `npm run verify`, and Browser smoke on `/modules/financial-goals` and `/lessons/goal-levels` passed.

T-043 removes redundant visible Finzdorov section codes from learner-facing module/unit path section headings without changing runtime JSON, content schema, API, auth, progress, or lesson reader behavior. Section titles now render as `Ваши базовые ценности`, `Видение будущего`, `Финансовые цели`, and `Мотивация достижения целей`, while the eyebrow remains `Раздел` without a numeric suffix. `npm run test:run -- src/App.test.tsx src/features/program-navigation/learningPath.test.ts`, `npm run verify`, and Browser smoke on `/modules/financial-goals` passed.

T-044 compacts `01.01 Ваши базовые ценности` from eight runtime lessons to four balanced lessons without changing schema, API, UI renderers, auth, or progress contracts. The section now contains `why-values-matter`, `what-are-values`, `values-conflict`, and `practice-1m`; deeper source slices remain documented under `docs/modules/module_1/lesson_01/` and supplemental metadata. `npm run check:content`, focused API/app tests, `npm run verify`, Browser smoke on `/modules/financial-goals`, `/lessons/why-values-matter`, and `/lessons/practice-1m` passed; local dev was restarted on backend `3002` and frontend `5175`.

T-045 polishes the module path sticky header without changing content, API, auth, progress, or routing contracts. The header back button now renders `Модуль 1 раздел N` for the section currently visible during scroll, the header title follows the visible section title, the title block has zero top margin, and the module button has comfortable horizontal padding. `npm run test:run -- src/App.test.tsx`, `npm run verify`, and Browser desktop/mobile smoke on `/modules/financial-goals` passed.

T-047 changes the authenticated entry behavior and account surface without adding profile management, achievements, followers, rewards, diagnostics, analytics, or content changes. Authenticated `/` now redirects to `/program`; the navigation label is `Профиль` and points to `/profile`; the old welcome screen is replaced by a Duolingo-like profile showing avatar area, email/login, registration date from `users.created_at`, shortened learner ID, and existing learning-progress stats only. Auth responses now include `createdAt`. Focused auth/app tests, `npm run verify`, and Browser desktop/390px smoke passed.

T-048 adds private saved answers for authenticated `reflection` and `artifact` cards without adding diagnostics, scoring, recommendations, analytics, rewards, or anonymous persistence. The backend exposes `GET /api/reflections` and `PUT /api/reflections/:cardId`, deriving ownership from the current session and storing neutral answer fields by `(user_id, card_id)`. The lesson reader requires meaningful reflection/artifact input before completion, saves the answer before marking the card complete, and leaves anonymous answers transient. `/profile` now includes `Мой финансовый ориентир`, grouped from existing unit context. `npm run check:content`, focused backend/frontend tests, `npm run verify`, and Browser 390px smoke passed.

T-049 adds `docs/QA_USER_SCENARIO_MAP.md`, a comprehensive QA scenario map for registration/login/logout, app shell navigation, program/module/unit paths, lesson/card flows, progress persistence, private reflection/artifact answers, profile display, API/session errors, accessibility, and mobile/desktop responsive coverage. No runtime code, content JSON, API, auth, progress, or profile behavior changed. `npm run verify` passed.

T-050 runs the full scenario QA pass against `docs/QA_USER_SCENARIO_MAP.md` without expanding MVP scope or changing content JSON. Browser QA covered P0 M-390/D-1440 flows, M-360 overflow, two-user privacy isolation, and full traversal of all 15 current lessons; API smoke covered content, protected endpoints, reflection validation, unknown lesson, and CORS preflight. Confirmed fixes make required completed progress writes fail closed, clear authenticated/private state on session-expired progress/reflection 401s, normalize non-JSON API errors, and show an empty program state when no modules are returned. Focused tests and `npm run verify` passed.

T-051 fixes mobile lesson CTA positioning on short cards without changing content, API, auth, progress, or routing contracts. The lesson session now uses a vertical flex layout so the existing sticky bottom action is pushed to the viewport bottom even when the active card content is shorter than the screen. `npm run test:run -- src/features/lesson-reader/LessonCardRenderer.test.tsx`, `npm run verify`, and Browser 390px/desktop smoke passed.

T-052 migrates backend persistence from SQLite/`better-sqlite3` to PostgreSQL/`pg` without changing frontend API contracts, product scope, or JSON content source-of-truth. The backend now uses an async repository boundary for users, sessions, progress, and private reflection answers; PostgreSQL DDL uses `uuid`, `text`, `timestamptz`, and `jsonb`; tests use isolated PostgreSQL schemas; CI provides a PostgreSQL service. `npm run typecheck`, `npm run lint`, focused backend tests, and `npm run verify` passed against PostgreSQL.

T-053 deploys the Yandex Cloud on-demand Managed PostgreSQL control layer. YC rejected uppercase `FinPulse`, so the target folder is `finpulse` (`b1gpl04msqva2tsff46k`). `finpulse-db` (`c9quhk2n9q3c3vvsp83g`) is PostgreSQL `16.13`, one public-IP host in `ru-central1-a`, `s2.micro`, `network-ssd`, 10 GB, with serverless access and dedicated security group `finpulse-db-sg` (`enpfi1mqc28vo7tc71kn`). Generated credentials are stored through Connection Manager/Lockbox metadata (`a59otq7kc4275f8onsdm` / `e6qdr1f6uh0k9aj2v34c`), without reading the payload. `finpulse-db-start` (`https://functions.yandexcloud.net/d4e0o3h9gnq59inscpns`) is public and opens/extends a 2-hour lease; `finpulse-db-autostop` runs from trigger `finpulse-db-autostop-5m` every 5 minutes. Verification invoked the public start URL once, confirmed `active_until=1780262473`, created the first required backup, manually stopped the cluster, and confirmed final status `STOPPED`. Full `npm run verify` remains blocked locally until a PostgreSQL test database URL is provided.

T-054 through T-060 add the production CI/CD deploy path. FinPulse now uses a same-origin single-container deployment shape: Fastify serves `/api/**` and the built Vite SPA from `dist/`; `Dockerfile`, `.dockerignore`, `vite.server.config.ts`, `npm run build:server`, `npm run build:container`, and `npm run start` define the production artifact. The backend exposes DB-free `/api/health` and DB-backed `/api/readyz`, can compose the PostgreSQL URL from env pieces plus `FINPULSE_DATABASE_PASSWORD`, and continues to run the idempotent `server/db/schema.sql` bootstrap at startup. Yandex deploy resources created on 2026-05-31: Container Registry `finpulse` (`crp5j8penr0hui0ttaum`), Serverless Container `finpulse-app` (`bbabho5nujsp32c8mvc7`, URL `https://bbabho5nujsp32c8mvc7.containers.yandexcloud.net/`), runtime SA `aje0lujm0q1obpn9fbu9`, deploy SA `ajeboe0h7j2k9vtfi06j`, WIF federation `ajeuttdtpqdudd97n6ei`, and federated credential `ajeci1l3l7qhus6vjqhk`. `.github/workflows/deploy.yml` runs on push to `main`: verify, GitHub OIDC to YC IAM token exchange, image build/push, DB start via `YC_DB_START_URL`, Serverless Container revision deploy with VPC network attachment and GitHub secret `FINPULSE_DATABASE_PASSWORD`, and smoke checks for frontend, `/api/health`, and `/api/readyz`. Operations runbook: `docs/operations/yandex-cloud-finpulse-deploy.md`. Local verification passed with a temporary PostgreSQL database: `npm run verify`, `npm run build:server`, and compiled production server smoke for `/`, `/api/health`, and `/api/readyz`. First `main` deploy runs found that YC rejects explicit `PORT`, single-zone `--subnets`, and preview `--secret` binding for this container; T-060 also added DB security-group ingress for the Serverless service subnet CIDR `198.19.0.0/16` and `FINPULSE_DATABASE_SSL_LIBPQ_COMPAT=true` for Yandex Managed PostgreSQL SSL.

T-061 runs full production scenario QA against the Yandex Serverless Container deployment. Production QA covered all P0 flows on M-390/D-1440, M-360 overflow, M-430/D-1024 responsive checks, keyboard/a11y smoke, API data boundaries, two-user privacy isolation, and full traversal of all 15 current runtime lessons. Confirmed fixes clear private UI after logout plus browser Back, preserve artifact template labels in profile answers from canonical content, make empty-JSON logout clear the session instead of returning 500, avoid viewed-progress writes for invalid lessons, improve key color-token contrast and compact touch targets, and make auth validation copy less misleading. `npm run verify` passed with a temporary local PostgreSQL database.

T-062 fixes intermittent production save failures after on-demand PostgreSQL stop/start events. Production logs showed `ETIMEDOUT` while the DB was starting and an unhandled idle `pg` pool `ECONNRESET` that could crash a serverless container invocation. The backend now handles idle pool errors without crashing, and the frontend retries idempotent `GET`/`PUT` requests briefly on transient `500`/`502`/`503`/`504` or browser network errors. Production API and Browser replay passed after starting the DB. `npm run test:run -- server/db/connection.test.ts src/App.test.tsx`, `npm run typecheck`, `npm run lint`, and `npm run build` passed. Full `npm run verify` was attempted but backend suites require `FINPULSE_TEST_DATABASE_URL`, `FINPULSE_DATABASE_URL`, or `DATABASE_URL` in the local shell.

T-063 performs the methodology reset requested on 2026-06-04. The new Google Docs methodology is imported as `docs/methodology/METHODOLOGY.md` and is now the central active methodology source; `AUTHORING.md`, `CONTENT_BACKLOG.md`, `README.md`, `AGENTS.md`, and `docs/CONTENT_MODEL.md` now point content/methodology work at the new source. Old active methodology/source directories and the previous runtime module are removed. Runtime content is replaced with `t1-start` / `money-and-operations` / `where-money-goes`, using only current card types: subjective `single_choice`, `theory`, objective `single_choice`, `artifact`, `reflection`, `artifact`, and `summary`. Profile saved answers now render under `Персональный финансовый навигатор` without old unit-slug grouping. `npm run check:content`, focused app tests, `npm run typecheck`, `npm run lint`, and `npm run build` passed; backend integration tests, Browser authenticated smoke, and full verify require a local PostgreSQL test database URL.

T-064 adds three exported Google Doc lessons to the active runtime after `where-money-goes`: `mandatory-and-desired`, `why-emergency-fund`, and `reserve-amount`. Source-only sorting, multi-select, calculation, reminder, psychotype-adaptive feedback, and video placeholders are adapted or deferred within current card types. T-065 supersedes the original T-064 placement of `why-emergency-fund` and `reserve-amount` inside `money-and-operations`.

T-065 splits the emergency-fund lessons into the correct second runtime unit: `planning-and-management` / `Юнит 2. Планирование и управление`. Source Markdown now lives under `docs/modules/t1-start/unit_02_planning_management/`; `src/content/modules/t1_start/module.json` references both unit files; `unit_01_money_and_operations.json` contains only `where-money-goes` and `mandatory-and-desired`; `unit_02_planning_and_management.json` contains `why-emergency-fund` and `reserve-amount`. `npm run check:content`, focused app/API tests, `npm run typecheck`, `npm run lint`, `npm run build`, full `npm run verify` with local PostgreSQL, and 390px Browser smoke passed.

T-066 removes confusing checkbox controls from interactive `artifact.template` rows. Template artifacts such as `Твои 3 траты за сегодня` now render as plain labeled text fields; real `checklist` cards keep checkbox behavior. `npm run test:run -- src/features/lesson-reader/LessonCardRenderer.test.tsx`, `npm run typecheck`, `npm run lint`, `npm run build`, full `npm run verify` with local PostgreSQL, and 390px Browser smoke passed.

T-067 cleans learner-facing wording in active T1 runtime lessons. Editor/source-adaptation sentences such as source-year caveats and missing-video/runtime notes were removed from runtime JSON while preserved source Markdown remains untouched. Local draft/completion status text for reflection/artifact cards is now `sr-only` instead of visible lesson content. `npm run check:content`, focused lesson-card tests, `npm run typecheck`, `npm run lint`, `npm run build`, full `npm run verify` with local PostgreSQL, and 390px Browser smoke passed.

T-068 polishes the shared lesson-reader layout without changing content JSON, schema, backend/API routes, auth, progress, or MVP scope. The progress header, compact lesson brief, card frame, sticky bottom action, and selectable option rows now share one mobile-first 480px lesson shell rhythm. Choice, checklist, and reflection options use a shared accessible selectable primitive with stable wrapping, focus, selected, correct, and retry states; a follow-up visual review fixed the option marker/text placement by switching the row to an explicit `20px + text` grid and keeping the native input absolutely hidden outside layout flow. Storybook now includes long Russian selected/correct/retry choice states and a viewport-safe lesson story frame. Focused lesson/app tests, `npm run typecheck`, `npm run lint`, `npm run build`, full `npm run verify` with local PostgreSQL, `npm run build:storybook`, Browser QA at 360/390/430/desktop, runtime selected-option QA at 390px, and Storybook 390px QA passed.

T-069 audits active T1 runtime learner-facing wording for awkward source adaptations. Hook-card feedback copied from internal methodology notes (`любой ответ принимается`, `вовлечение`, `не проверка`) was removed from active runtime JSON; the awkward `ничего не нарушил(а)` reflection guidance was rewritten into a neutral observation-focused message; malformed/gendered reflection prompts, options, and prominent summary points were tightened without changing schema, API, UI renderers, auth, progress, or source Markdown. `npm run check:content`, focused app/lesson-card tests, `npm run typecheck`, `npm run lint`, `npm run build`, full `npm run verify` with local PostgreSQL, and Browser smoke on the live `where-money-goes` reflection card passed.

T-070 makes the shared lesson intro/goal brief more explicit without changing JSON content, schema, backend/API routes, auth, progress, or lesson flow. `LessonBrief` now renders a highlighted focus surface with current FinPulse brand/sky tokens, visible `В этом уроке` and `Цель урока` labels, a target icon callout, and compact mobile typography; all lessons with `description` and `learningGoal` inherit the treatment. Focused lesson-reader tests, `npm run typecheck`, `npm run lint`, `npm run build`, full `npm run verify` with local PostgreSQL, and Browser 360/390px smoke on `/lessons/where-money-goes` passed with no horizontal overflow or console warnings/errors.

T-071 adds explicit custom input support for subjective single-select reflection cards without changing backend/API/database/auth/progress behavior. `reflection.customOption` is documented, validated in the JSON schema, zod model, and manual content validator, and guarded for `inputType: "single_select"` only. The three current learner-facing `Свой вариант` rows moved out of `options` into `customOption`; the lesson reader now shows an accessible text field when that row is selected, keeps `Далее` disabled until typed text is present, and persists only the typed value as `singleValue`. `npm run check:content`, focused lesson-reader tests, `npm run typecheck`, `npm run lint`, `npm run build`, full `npm run verify` with local PostgreSQL, and Browser 390px smoke on `/lessons/where-money-goes` passed.

T-072 removes the awkward `Мягче:` prefix from runtime artifact variants in active T1 lessons and keeps preserved source Markdown unchanged. Artifact variant buttons now wrap within the lesson card with left-aligned text and no horizontal overflow, without changing content schema, API, backend, auth, progress, shared shadcn `Button`, or lesson flow. `npm run check:content`, `npm run test:run -- src/App.test.tsx`, `npm run typecheck`, `npm run lint`, `npm run build`, full `npm run verify` with local PostgreSQL, runtime `Мягче:` search, and Browser 360/390px smoke on `/lessons/where-money-goes` passed.

T-073 adds explicit FinPulse design-scale tokens for spacing, radius, and mobile typography in `src/index.css`, exposes spacing/radius scales to Tailwind v4 theme variables, and adds the standalone `/design/lesson-block-variants` preview route with three redesigned variants of the `Деньги утекают по капле` lesson theory block. The production lesson reader remains unchanged until a variant is selected. `npm run test:run -- src/App.test.tsx`, `npm run typecheck`, `npm run lint`, `npm run build`, full `npm run verify` with local PostgreSQL, and Browser desktop/390px smoke on `/design/lesson-block-variants` passed with no horizontal overflow or console warnings/errors.

T-074 adds optional `artifact.customOption` to the content contract for artifact cards with `variants`, without changing backend/API/database/auth/progress behavior or the persisted answer payload shape. The two current Unit 1 micro-rule cards (`Твоё правило на 3 дня` and `Твоё правило выбора`) now render variants as native radio rows plus a required typed `Свой вариант`, reusing the shared lesson `SelectableOption` primitive and saving custom text through existing `selectedVariant`. Artifact cards without `customOption` keep the existing variant-button behavior. `npm run check:content`, focused lesson/app tests, `npm run typecheck`, `npm run lint`, `npm run build`, full `npm run verify` with local PostgreSQL, and standalone Chrome/Playwright 390px smoke on both Unit 1 micro-rule screens passed with no post-login console/page errors or horizontal overflow. In-app Browser was attempted first, but text entry was blocked by its missing virtual clipboard, so standalone Chrome was used for the final interactive smoke.

T-075 applies the selected lesson-block variant 2 without icons to passive explanatory lesson cards. `theory`, `callout`, `summary`, read-only `scenario`, and read-only `single_choice` now share a tokenized explanatory surface pattern: card-frame title only, body text in design-scale typography, fact/formula/test/example paragraphs in soft insight panels, structured calculation cells for the recognized current formulas, and text-only example chips. The preview route keeps `/design/lesson-block-variants` but variant 2 is now icon-free. `docs/DESIGN_SYSTEM.md` documents this rule as `9.3.1. Explanatory Calculation Block`. Focused lesson/app tests, full `npm run verify` with local PostgreSQL, and Browser 390px smoke on the preview, `where-money-goes`, and `reserve-amount` passed with no card SVG, no horizontal overflow, and no console warnings/errors.

T-076 applies the user-approved vertical-step solution to mobile calculation blocks. Calculation steps now render as a single DOM sequence `step -> operator -> step -> result`: on mobile this is a vertical stack, while from the `sm` breakpoint upward the same sequence remains a compact horizontal formula grid. The production theory cards and `/design/lesson-block-variants` preview no longer produce the previous mobile zigzag or duplicated operator text such as `××` / `==`. `docs/DESIGN_SYSTEM.md` now states that calculation surfaces use vertical stacks on mobile and horizontal formulas from `sm` upward. Focused lesson/app tests, typecheck, lint, build, full `npm run verify` with local PostgreSQL, and Browser 390px smoke on `where-money-goes`, `reserve-amount`, and the preview passed with strictly vertical steps, no duplicated operators, no horizontal overflow, and no console warnings/errors.

T-077 updates the T-076 calculation layout to be container-aware instead of viewport-breakpoint based. Calculation blocks now use a native CSS container query: vertical `step -> operator -> step -> result` is the default in narrow lesson cards, and the horizontal formula grid is enabled only when the calculation container is at least `36rem` wide. The production `TheoryCard` and `/design/lesson-block-variants` preview share the same `fr-calculation-*` classes. Focused lesson/app tests, typecheck, lint, build, full `npm run verify` with local PostgreSQL, and Browser smoke on `reserve-amount`, `where-money-goes`, and the preview passed with no horizontal overflow or console warnings/errors.

T-078 adds objective practice card types accepted by ADR-0009. Runtime JSON now supports `multi_select` for several correct options and `categorization` for assigning items to known categories. The lesson reader renders both as accessible mobile-first controls with local checked-answer feedback; selected answers are not persisted through `/api/reflections`. Active practice screens migrated: `where-money-goes` card 3 and `mandatory-and-desired` card 3 now use `categorization`; `why-emergency-fund` card 3 now uses `multi_select`; existing card ids were preserved for progress keys. `npm run check:content`, focused schema/reader/app/backend tests, typecheck, lint, build, full `npm run verify` with local PostgreSQL, `npm run build:storybook`, and Browser 390px smoke on all three migrated lessons passed.

T-079 adds the standalone `/design/practice-card-variants` preview page for choosing the future UX direction of the `where-money-goes` categorization practice. The page contains five interactive mobile-first variants: row switches, one-item focus, two buckets without drag-and-drop, card deck, and compact matrix. Production lesson reader behavior remains unchanged until a variant is selected. Focused App tests, typecheck, lint, build, full `npm run verify` with local PostgreSQL, and Browser 390px/1280px smoke passed with no console warnings/errors or horizontal overflow.

T-080 reframes `/design/practice-card-variants` from five categorization-specific designs into five reusable practice patterns: list actions, one-object focus, grouped zones, card flow, and tabular choice. Each pattern now shows compact fit chips for relevant exercise types while still using `where-money-goes` as the concrete example. Production lesson reader behavior remains unchanged. Focused App tests, typecheck, lint, build, full `npm run verify` with local PostgreSQL, and Browser 390px/1280px smoke passed with no console warnings/errors or horizontal overflow.

T-081 applies the selected card-flow practice pattern to production choice-like lesson cards and removes the temporary `/design/practice-card-variants` page. Interactive `single_choice` / `scenario`, `multi_select`, and `categorization` cards now present one option or item in focus with compact icon navigation and progress dots, while keeping the existing bottom `Проверить` / `Далее` behavior and checked-answer feedback. Source Markdown for U1.1, U1.2, U2.1, and U2.2 was reviewed; runtime JSON already matched the exercises, so content data did not change. Focused tests, lint, content validation, and full `npm run verify` passed with `FINPULSE_TEST_DATABASE_URL=postgres://finpulse:finpulse@127.0.0.1:5432/finpulse`; the first verify attempt without a DB URL failed at backend test setup only. Browser 390px smoke passed on `where-money-goes`, `mandatory-and-desired`, `why-emergency-fund`, `reserve-amount`, and the removed design route redirect with no horizontal overflow or console warnings/errors. After a final progress-dot `flex-wrap` hardening, full verify passed again; a repeat in-app Browser smoke/reset attempt timed out in the Browser runtime.

T-082 narrows the T-081 card-flow rollout to the intended objective third-screen practice cards only. Ordinary hook choices, subjective choices, and scenario choices render as the previous list-style selectable rows again, while third-screen `categorization`, `multi_select`, and objective `single_choice` exercises keep the approved card-flow pattern with accessible controls. `docs/DESIGN_SYSTEM.md` now documents this scope explicitly. Focused lesson/app tests and full `npm run verify` passed with local PostgreSQL. Local development was restarted on Vite `http://127.0.0.1:5174/` and API `http://127.0.0.1:3001`; Browser 390px smoke passed on `where-money-goes`, `mandatory-and-desired`, `why-emergency-fund`, and `reserve-amount` with no horizontal overflow or console warnings/errors.

T-083 restores the separate `/design/practice-card-variants` experiment page for discussing third-screen practice UX independently from production lesson-reader behavior. The page uses `where-money-goes` card 3 as the concrete exercise and compares five patterns: list actions, one-object focus, grouped zones, card flow, and matrix. Production lessons were not changed. Focused App tests, typecheck, lint, full `npm run verify` with local PostgreSQL, and Browser 390px smoke passed with no horizontal overflow or console warnings/errors.

T-084 redesigns the first four `/design/practice-card-variants` scenarios from scratch using design/frontend-development guidance while keeping the experiment page scoped away from production lessons. The page now compares a decision feed, one-object stepper, two-stack assignment flow, cleaned card flow, and the preserved matrix variant; the matrix also received a small mobile column-width polish so labels fit in the preview shell. `npm run test:run -- src/App.test.tsx`, `npm run typecheck`, `npm run lint`, full `npm run verify` with local PostgreSQL, and Browser 390px smoke passed with no horizontal overflow or console warnings/errors.

T-085 adds a sixth `/design/practice-card-variants` experiment for the requested card-flow/matrix hybrid. The new variant keeps a much smaller focused card for choosing the current expense category and shows an always-visible matrix of all expenses underneath; choosing on the card updates the matrix immediately and advances to the next item. Production lessons and content JSON were not changed. `npm run test:run -- src/App.test.tsx`, `npm run typecheck`, `npm run lint`, full `npm run verify` with local PostgreSQL, and Browser 390px smoke passed with no horizontal overflow or console warnings/errors.

T-086 adds a seventh `/design/practice-card-variants` experiment for auto-advancing card flow. The new variant keeps cards close to variant 4, removes manual slider arrows, advances to the next card immediately after selecting a category, and shows the result table plus enabled `Проверить` only after all five items are answered. Production lessons and content JSON were not changed. `npm run test:run -- src/App.test.tsx`, `npm run typecheck`, `npm run lint`, full `npm run verify` with local PostgreSQL, and Browser 390px smoke passed with no horizontal overflow or console warnings/errors.

T-087 makes the final table in the seventh `/design/practice-card-variants` experiment editable. After all auto-flow cards are answered, category cells in the result table are accessible buttons with `aria-pressed`; switching a cell updates the assignment in-place and keeps `Проверить` enabled. Production lessons and content JSON were not changed. `npm run test:run -- src/App.test.tsx`, `npm run typecheck`, `npm run lint`, full `npm run verify` with local PostgreSQL, and Browser 390px smoke passed with no horizontal overflow or console warnings/errors.

T-089 polishes the seventh `/design/practice-card-variants` auto-flow card presentation. Active cards are more compact, no longer show the expense icon, and use tighter counter/title spacing. The category buttons in variant 7 now use compact typography plus visible hover, focus-visible, and press-scale affordances while preserving the auto-advance behavior. Production lessons and content JSON were not changed. `npm run test:run -- src/App.test.tsx`, `npm run typecheck`, `npm run lint`, full `npm run verify` with local PostgreSQL, and Browser 390px smoke passed with no horizontal overflow or console warnings/errors.

T-090 adds a page-turn-like transition to the seventh `/design/practice-card-variants` auto-flow card sequence. Selecting a category now starts a short exit/enter animation with perspective, rotation, and horizontal movement before the next card becomes active; buttons are temporarily disabled during the transition and reduced-motion users skip the animation. Production lessons and content JSON were not changed. `npm run test:run -- src/App.test.tsx`, `npm run typecheck`, `npm run lint`, full `npm run verify` with local PostgreSQL, and Browser 390px smoke passed with no horizontal overflow or console warnings/errors.

T-091 fixes clipped-looking shadows on the rounded auto-flow cards in the seventh `/design/practice-card-variants` experiment. The auto-card stage no longer applies rectangular `overflow-hidden`; it now provides a small padding buffer, visible overflow, larger stage radius, and isolation so the rounded card shadow paints naturally around the corners without creating horizontal page overflow. Production lessons and content JSON were not changed. `npm run test:run -- src/App.test.tsx`, `npm run typecheck`, `npm run lint`, full `npm run verify` with local PostgreSQL, and Browser 390px smoke passed with no horizontal overflow or console warnings/errors.

T-092 applies the approved seventh auto-advancing card-flow pattern to production third-screen `categorization` practice only. `where-money-goes` card 3 and `mandatory-and-desired` card 3 now show one item card at a time, advance immediately after a category choice, then show an editable final result table before `Проверить`; table edits reset checked state through the existing categorization state handler. `multi_select`, `single_choice`, read-only cards, non-third-screen categorization cards, content JSON, and `card.id` values were not changed. Focused lesson-reader tests, app tests, typecheck, lint, full `npm run verify` with local PostgreSQL, and Browser 390px smoke on both affected lessons passed with no horizontal overflow or console warnings/errors. Local development was restarted on `http://localhost:5173/` with the API on `http://127.0.0.1:3001`.

T-093 removes the duplicated lesson description from the first lesson-reader screen and makes `learningGoal` the single standalone `Цель урока` intro card. Runtime JSON, content schema, backend/API, progress, and lesson flow were not changed; `lesson.description` remains available for navigation surfaces. Focused lesson-reader tests, full `npm run verify` with local PostgreSQL, and Browser 390px smoke on `mandatory-and-desired` passed with no horizontal overflow or console warnings/errors.

T-094 converts `why-emergency-fund` card 3, `Где подушка реально выручает?`, from the older multi-select presentation to two-category `categorization`: `Подушка помогает` and `Не для подушки`. The existing card id is preserved for progress continuity, item feedback is retained, and the screen now uses the approved auto-advancing focused card plus editable result table before `Проверить`. `npm run check:content`, focused lesson-reader/API tests, full `npm run verify` with local PostgreSQL, and Browser 390px smoke on a temporary fresh dev pair passed with no horizontal overflow or console warnings/errors.

T-095 converts `reserve-amount` card 3, `Проверь расчёты подушки`, from third-screen objective `single_choice` to `categorization` with three formula-check examples and categories `Верно` / `Есть ошибка`. The existing card id is preserved for progress continuity, no renderer/schema/backend behavior changed, and the screen now uses the approved auto-advancing focused card plus editable result table before `Проверить`. `npm run check:content`, focused lesson-reader tests, full `npm run verify` with local PostgreSQL, and Browser 390px smoke on a temporary fresh dev pair passed with no horizontal overflow or console warnings/errors.

T-096 adds shared no-break rendering for financial and duration fragments in lesson-card text. Visible strings such as `40 000 ₽ в месяц`, `6 месяцев`, `200 000 ₽`, `3–6 зарплат`, and similar amount/unit phrases are wrapped in `whitespace-nowrap` spans across card renderers, while preserving source JSON and interaction behavior. Focused lesson-reader tests, typecheck, content validation, lint, full `npm run verify` with local PostgreSQL, and Browser 390px/360px QA on `reserve-amount` card 3 passed with no split amount/unit fragments, no horizontal overflow, and no console warnings/errors.

T-097 aligns current Unit 2 fourth screens with the personal real-data working pattern used in Unit 1. `why-emergency-fund` screen 4 is now a personal emergency-situation `artifact` instead of a checked external scenario; `reserve-amount` moved the target-amount calculation artifact to screen 4 and made screen 5 a reflection on how the amount feels. `docs/methodology/AUTHORING.md` now documents the T1 rule: screen 4 should be a personal real-world artifact when a plausible learner-owned data point exists, screen 5 carries reflection, and screen 6 fixes the micro-rule or first step. `npm run check:content`, focused lesson-reader/backend/content-contract tests, and full `npm run verify` passed with local PostgreSQL.

T-098 aligns the current Unit 2 final micro-rule screens with the Unit 1 artifact variant pattern. `why-emergency-fund` screen 6 now offers two ready purpose formulations plus `Свой вариант`; `reserve-amount` screen 6 now offers two first-step rules plus `Свой вариант` and no longer repeats target-amount entry. The Unit 2 source Markdown was updated to match runtime JSON. `npm run check:content`, focused lesson-reader/app tests, and full `npm run verify` passed with local PostgreSQL.

T-099 replaces the compact one-line custom-option inputs in reflection and artifact cards with a shared textarea-like `Мой вариант` field aligned with the screen-four personal entry pattern. The field preserves content placeholders, autofocuses after selecting `Свой вариант`, keeps existing reflection/artifact payload behavior, and is covered by focused tests plus Browser 390px smoke on `why-emergency-fund` screens 5 and 6. Focused lesson-reader tests, typecheck, lint, build, full `npm run verify` with local PostgreSQL, and Browser smoke passed with no console warnings/errors or horizontal overflow.

T-100 expands the first four active T1 lessons to eight screens by adding a new fourth external-example `scenario` with three options, objective check feedback, and the source-backed statistics previously attached to the personal-work cards. The old personal-work artifact cards are now screen 5, while reflection, micro-rule, and summary cards shift to screens 6-8 with their existing ids preserved. Source Markdown, runtime JSON, and `docs/methodology/AUTHORING.md` now document the new T1 ladder: screen 4 external example with statistics, screen 5 personal work. `npm run check:content`, focused lesson-reader/app/content-contract/backend tests, full `npm run verify` with local PostgreSQL, and Browser 390px smoke on all four lessons passed with no console warnings/errors or horizontal overflow.

T-101 removes confirmed dead and stale project surfaces from the audit while explicitly preserving `src/shared/ui/Mascot.tsx` and `public/assets/mascot/**`. Production no longer exposes `/design/lesson-block-variants` or `/design/practice-card-variants`; the old experiment pages, story-only navigation components, `src/content/loadProgram.ts`, `src/content/selectors.ts`, `server/db/index.ts`, unused repository/content-service methods, unused `zustand`, stale scaffold snippet, obsolete wordmark/source brand artifacts, and local sqlite data are removed. Test-only content hydration now lives under `src/test/loadProgram.ts`, pure ordering helpers live in `src/content/order.ts`, docs were updated to the current four-lesson runtime graph, and `npm run verify` now includes `npm run build:storybook`. Full `FINPULSE_TEST_DATABASE_URL=postgres://finpulse:finpulse@127.0.0.1:5432/finpulse_test npm run verify` passed after creating the documented local test database.

T-102 adds the standalone `/design/lesson-completion-variants` experiment page for comparing four possible completed-lesson directions before changing production `LessonSession`: `Тихий финиш`, `Отметка маршрута`, `Правило с собой`, and `Компас отметил финиш`. Four read-only subagents contributed bounded concepts, while the orchestrator integrated the route, responsive preview page, existing `Mascot` placements, restrained CSS victory animations with reduced-motion fallback, and focused App coverage. Production lesson completion, content JSON, backend, auth, progress, rewards, diagnostics, analytics, and external assets were not changed. Focused App test, typecheck, lint, build, full verify with local PostgreSQL URL, and Browser 390px/1280px smoke passed with no console warnings/errors or horizontal overflow.

T-103 adds the fifth `/design/lesson-completion-variants` option requested from user feedback. The new `Финиш с маскотом` variant combines the first variant's compact completion structure with the fourth variant's centered mascot/halo treatment, changes the completion heading to `Урок пройден`, centers the heading and description under the mascot, and uses `К следующему уроку` / `К списку уроков` CTAs. Production `LessonSession`, content JSON, backend, progress, rewards, diagnostics, and analytics were not changed. Focused App test, typecheck, lint, full verify with local PostgreSQL URL, and Browser 390px smoke passed with five variants, five mascot images, no console warnings/errors, and no horizontal overflow.

T-104 applies the approved fifth completion variant to production `LessonSession` and removes the temporary `/design/lesson-completion-variants` experiment route/page. Completed lessons now show the centered mascot/halo composition, heading `Урок пройден`, centered description, completed-card progress, and `К следующему уроку` / `К списку уроков` actions when a next lesson exists; final lessons show `К списку уроков`. Focused App/lesson-reader tests, typecheck, lint, full verify with local PostgreSQL URL, and Browser 390px completion/redirect smoke passed with no console warnings/errors or horizontal overflow.

T-105 replaces app-facing exact `FinPulse` brand strings with `ФинПульс` across the SPA shell, auth entry, runtime content metadata, mascot alt text, Storybook foundation labels, and test expectations. Exact `FinPulse` remains only in technical or historical docs/harness/deploy records outside the app-facing brand surface. `npm run check:content`, focused App/logout tests, and full verify with local PostgreSQL URL passed; plain `npm run verify` without a DB URL still fails at the documented backend test precondition.

T-088 makes methodologist `Блок статистики` / `Статистика по теме` sections a first-class card-level `statistics` field. The zod content model, JSON schema, and manual content validator now accept structured statistics, and `npm run check:content` requires `card.statistics` when a card's `sourceSection` points to a source Markdown screen containing `Блок статистики`. The current four T1 screen-4 cards now include their source statistics and the lesson reader renders them inside the card. Authoring docs now require this conversion for future scripted lessons. Focused lesson-reader tests, content validation, typecheck, lint, build, full `npm run verify` with local PostgreSQL, and Browser 390px smoke on a fresh temporary dev pair passed.

## State update rules

Update this file when:
- the stack changes;
- app scaffold is created;
- core routes are implemented;
- content model changes;
- verification commands change;
- evals start being populated;
- major scope decisions are made.

Do not use this file as a detailed task log. Use `harness/WORKBOARD.md` and task files for that.
